---
phase: quick-260905-f0r
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [M2-READ-01, M2-DESIGN-01]
files_modified:
  - package.json
  - package-lock.json
  - ios/Podfile.lock
  - src/stores/readerChromeStore.ts
  - src/navigation/TabNavigator.tsx
  - src/navigation/TabBarSelection.tsx
  - src/navigation/readerTabBarMotion.ts
  - src/navigation/readerTabBarMotion.test.ts
  - src/navigation/tabNavigatorSource.test.ts
  - src/navigation/tabBarCapsuleStyle.ts
  - src/navigation/tabBarVisibility.test.ts
  - src/hooks/useTabBarHeight.ts
  - src/hooks/useTabBarHeightSource.test.ts
  - src/screens/bible/BibleReaderScreen.tsx
  - src/screens/bible/readerChromeMotion.ts
  - src/screens/bible/readerChromeMotion.test.ts
  - src/screens/bible/bibleReaderChromeSource.test.ts
  - src/components/audio/ReaderPlaybackDock.tsx
  - src/components/audio/readerPlaybackDockSource.test.ts
must_haves:
  truths:
    - Reader resting controls match the target geometry and play remains the same size throughout collapse.
    - Reader dock and tab capsule move continuously from one UI-thread progress value, including direction reversal.
    - Supported iOS displays native glass without fading a glass ancestor; unsupported systems retain readable controls.
    - Existing navigation, playback, plan completion, locales, and accessibility remain functional.
  artifacts:
    - path: src/stores/readerChromeStore.ts
      provides: Nonpersisted shared animation progress contract
      exports: [useReaderChromeProgress]
    - path: src/navigation/TabNavigator.tsx
      provides: Native material and continuously animated tab capsule
    - path: src/components/audio/ReaderPlaybackDock.tsx
      provides: Constant-size reference transport controls
    - path: src/screens/bible/BibleReaderScreen.tsx
      provides: UI-thread scroll-to-chrome wiring
  key_links:
    - from: src/screens/bible/BibleReaderScreen.tsx
      to: src/stores/readerChromeStore.ts
      via: useReaderChromeProgress shared value written in scroll worklet
      pattern: useReaderChromeProgress
    - from: src/navigation/TabNavigator.tsx
      to: src/stores/readerChromeStore.ts
      via: animated wrapper consumes the same progress
      pattern: useReaderChromeProgress
    - from: src/screens/bible/BibleReaderScreen.tsx
      to: src/components/audio/ReaderPlaybackDock.tsx
      via: coordinated dock translation and transport handlers
      pattern: ReaderPlaybackDock
---

<objective>
Reproduce the supplied target reader's native material, geometry, and coordinated scroll collapse while retaining current app behavior.
Purpose: Remove the visible snap, independently moving controls, and oversized ring that prevent parity.
Output: Integrated native glass and reader motion with deterministic regressions and native visual evidence.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/quick/260905-f0r-match-reader-controls-and-liquid-glass-t/260905-f0r-CONTEXT.md
@src/navigation/TabNavigator.tsx
@src/navigation/tabBarVisibility.ts
@src/hooks/useTabBarHeight.ts
@src/screens/bible/BibleReaderScreen.tsx
@src/components/audio/ReaderPlaybackDock.tsx

<interfaces>
New agreed contract: `src/stores/readerChromeStore.ts` stores nonpersisted Reanimated `makeMutable` values; `useReaderChromeProgress(): SharedValue<number>` returns root progress, 0 expanded and 1 collapsed, and `useReaderChromeOwner(): SharedValue<string>` identifies the owning reader route key. Each retained reader keeps local progress. All per-frame writes remain on the UI thread and only the owning focused reader can publish root progress; delayed JS callbacks and focus cleanup also check ownership. Cleanup cannot reset a newer reader's state. Preserve the last actual scroll offset when refocusing retained content, or seed the first event without computing a false delta from zero. Reset chapter-local motion when book/chapter content changes. Forced route hiding takes precedence over motion. Tab wrapper consumes progress only while Bible/BibleReader is active.

Existing geometry: `useTabBarHeight(): TabBarHeightMetrics` returns `bottomPadding`, `barHeight`, `sideInset`, and `height`; `buildTabBarCapsuleStyle({sideInset, bottomPadding, barHeight, collapseProgress?})` provides the common capsule geometry. Native glass consumers must not inherit its existing progress opacity.

Existing dock props include `collapseProgress`, `isCollapsed`, `progress`, `isPlaying`, `isLoading`, chapter availability and callbacks, optional `hidePlayButton`, and plan-specific next-control labels/icons/colors. Keep action semantics; remove only obsolete decorative ring inputs if all call sites are updated together.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish shared progress and native tab material</name>
  <files>src/stores/readerChromeStore.ts, src/navigation/TabNavigator.tsx, src/navigation/TabBarSelection.tsx, src/navigation/readerTabBarMotion.ts, src/navigation/readerTabBarMotion.test.ts, src/navigation/tabNavigatorSource.test.ts, src/navigation/tabBarCapsuleStyle.ts, src/navigation/tabBarVisibility.test.ts, src/hooks/useTabBarHeight.ts, src/hooks/useTabBarHeightSource.test.ts, package.json, package-lock.json, ios/Podfile.lock</files>
  <behavior>Reference capsule is 60 points high with 21-point horizontal inset and about 22-point bottom gap on the measured device; selected segment is about 84 × 54. Active reader progress translates the capsule continuously through approximately 132 points. Other routes do not inherit stale collapse; forced-hidden routes stay hidden. Native glass ancestry never fades.</behavior>
  <action>Per D-01 through D-05, define the transient Zustand shared-value contract first, then wrap the existing bottom tab bar with a UI-thread animated container. Retain React Navigation events, selected state, haptics, existing labels, Bible resume and Plans reset handlers. Consume progress only for active Bible/BibleReader when route visibility permits; use existing route policy for plan/selection hiding. Add the SDK-compatible Expo glass dependency by merging existing package changes, install native pods locally, and choose GlassView only when supported; retain blur fallback. Set the native material to the verified clear style without opaque fill overlays and keep it and ancestor opacity 1. Apply theme-aware foregrounds and reference-sized selected segment styling. Do not add a Context provider. This slice owns navigation and material wiring; coordinate its interface with Task 2 before either changes the contract.</action>
  <verify><automated>node --test --import tsx src/navigation/readerTabBarMotion.test.ts src/navigation/tabBarVisibility.test.ts src/navigation/tabNavigatorSource.test.ts src/hooks/useTabBarHeightSource.test.ts</automated></verify>
  <done>The shared-value store contract is available, existing tab behaviors remain intact, capsule geometry matches the reference, and supported native material has no fading ancestor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive reader and transport from continuous scroll motion</name>
  <files>src/screens/bible/BibleReaderScreen.tsx, src/screens/bible/readerChromeMotion.ts, src/screens/bible/readerChromeMotion.test.ts, src/screens/bible/bibleReaderChromeSource.test.ts, src/components/audio/ReaderPlaybackDock.tsx, src/components/audio/readerPlaybackDockSource.test.ts</files>
  <behavior>Progress stays in [0,1], changes at intermediate scroll offsets, responds continuously on reversal even deep in a chapter, clamps top/bottom overscroll, and resets on reader departure. Regression sequence: collapse reader A, focus reader B or plan reader, deliver A's late scroll/JS callback and cleanup, and verify B remains unchanged. Return to A at its retained deep offset and drag one point: progress changes by that point, without a jump caused by zeroed offset. Changing book/chapter resets the corresponding local motion. Transports share progress with target-specific travel distances, play does not scale with collapse, and no progress ring remains. Selection/plan hiding and read/listen continuity remain functional; hidden controls cannot be activated or focused by accessibility services.</behavior>
  <action>Per D-01 and D-03 through D-06, write focused regressions first, then update collapse progress directly in the existing Reanimated scroll worklet, guarded by reader focus. Remove the 48-point JS notification dependency and binary root-tab threshold from visual motion. Keep JS crossings only for meaningful semantic transitions that require React state or navigation. Derive stable safe-area-aware anchors matching approximate expanded play center y=824 and collapsed center y=889 at 440×956; retain constant 64-point play size and 40-point visible side controls at x=44/396, y=837 with sufficient touch area. Drive the measured 65-point play travel and 132-point side-control/capsule travel from shared progress, without fading or growth. Remove the decorative ring and old extra per-control scale offsets. Native glass on reader controls must keep opacity 1 throughout its ancestry; existing non-glass content can retain appropriate opacity transitions. Preserve selection restore, forced plan hiding, route cleanup, all callbacks, audio state, and localized labels. For reduced motion avoid spring/scale embellishments while maintaining coherent control positions and availability. Keep content padding stable so control changes cannot reflow a reader at chapter end.</action>
  <verify><automated>node --test --import tsx src/screens/bible/readerChromeMotion.test.ts src/screens/bible/bibleReaderChromeSource.test.ts src/components/audio/readerPlaybackDockSource.test.ts</automated></verify>
  <done>Dock and tab use the same frame-by-frame progress, controls match measured geometry without ring/growth, reversal/overscroll behavior is deterministic, and existing actions remain reachable.</done>
</task>

<task type="auto">
  <name>Task 3: Verify integration and native parity</name>
  <files>.planning/quick/260905-f0r-match-reader-controls-and-liquid-glass-t/260905-f0r-SUMMARY.md, .planning/quick/260905-f0r-match-reader-controls-and-liquid-glass-t/260905-f0r-VERIFICATION.md</files>
  <action>Per D-04 through D-06, run focused regressions followed by the repository verification gate and full tests. Validate all 21 locale coverage/rendering checks. Build and launch the integrated native app locally on the available iPhone 17 Pro Max target; identify whether evidence is simulator or physical device. Capture resting, mid-collapse, fully collapsed, upward reversal, top and bottom overscroll, and return-to-another-tab states; compare against the target recording with viewport-normalized measurements. Exercise play/pause, chapter arrows, verse selection/dismissal, plan completion, Bible resume, and Plans reset. Check reduced motion, accessibility focus/touch exclusion for hidden controls, light/dark themes, and an unsupported-glass platform path. Record the exact build/source state and evidence paths. If a native target is unavailable, record that gate as unresolved rather than treating source tests as visual proof. Preserve unrelated WIP and report any unrelated baseline gate failure separately.</action>
  <verify><automated>npm run release:verify && npm test</automated></verify>
  <done>Automated gates pass or exact pre-existing blockers are isolated; native evidence records geometry/motion/material and behavioral checks, with any unverified target explicitly identified.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Scroll events → UI state | Native offsets and dimensions enter the collapse calculation. |
| Route state → interaction | Nested route state determines whether controls are reachable. |
| OS capability → material | Runtime native availability selects glass or fallback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-260905-01 | D | Reader scroll handler | mitigate | Clamp progress and overscroll; avoid per-frame React/navigation mutations. |
| T-260905-02 | T | Animated tab/dock wrappers | mitigate | Preserve forced route hiding and disable touch/accessibility reachability when hidden. |
| T-260905-03 | D | Native material branch | mitigate | Runtime capability guard and existing unsupported-platform fallback; prove native build compatibility. |
| T-260905-04 | I | Visual evidence | mitigate | Capture only requested app states and keep unrelated account or workspace content out of artifacts. |
</threat_model>

<verification>
Focused Node/tsx regressions, `npm run release:verify`, `npm test`, and native recorded interaction. Match reference proportions at equal logical viewport before tuning device-specific offsets. No publishing is part of this plan.
</verification>

<success_criteria>
One continuous motion contract drives the reader and tabs; constant-size ring-free controls and real supported native glass reproduce the target; existing navigation/audio/plan/localization behavior passes verification. Native parity claims must cite actual capture evidence.
</success_criteria>

## Dependencies and coverage

Task 1 creates the contract/material/tab implementation; Task 2 consumes the contract and owns reader/dock files. Their separate file ownership permits parallel implementation after agreeing the contract. Task 3 depends on both integrated slices. No human checkpoint is required.

| Source | Item | Coverage |
| --- | --- | --- |
| GOAL | Latest recording-led reader parity | Tasks 1–3 |
| REQ | M2-READ-01 read/listen continuity | Tasks 2–3 |
| REQ | M2-DESIGN-01 consistent chrome and geometry | Tasks 1–3 |
| RESEARCH | Expo native glass compatibility, runtime check, opacity constraint | Tasks 1–3 |
| CONTEXT | D-01 reference geometry/motion | Tasks 1–3 |
| CONTEXT | D-02 native material | Tasks 1–3 |
| CONTEXT | D-03 current navigation/shared animation | Tasks 1–2 |
| CONTEXT | D-04 existing behavior | Tasks 1–3 |
| CONTEXT | D-05 locale/accessibility/theme | Tasks 1–3 |
| CONTEXT | D-06 deterministic and native verification | Tasks 2–3 |

This quick task refines the previously completed Phase 12.1 surface under the latest user reference; its old shrinking chapter-pill design does not override the supplied constant-size play target. No deferred ideas are planned.

<output>
After completion, create `.planning/quick/260905-f0r-match-reader-controls-and-liquid-glass-t/260905-f0r-SUMMARY.md` and `260905-f0r-VERIFICATION.md` with exact validation results and evidence paths.
</output>
