---
phase: 35-signup-simplification-and-deferred-auth-conversion
plan: 03
type: execute
wave: 3
depends_on:
  - 35-02
files_modified:
  - src/navigation/rootNavigation.ts
  - src/screens/more/MoreScreen.tsx
  - src/screens/more/ProfileScreen.tsx
  - src/screens/learn/GroupListScreen.tsx
  - src/screens/learn/PrayerWallScreen.tsx
  - docs/release-smoke-checklist.md
  - src/screens/more/moreScreenSource.test.ts
autonomous: true
requirements: [SYNC-02, AUTH-01]
user_setup: []
must_haves:
  truths:
    - "Guest users can still browse and read without new auth walls."
    - "The highest-signal guest surfaces explain why auth helps instead of forcing it during onboarding."
    - "At least the primary account-bound guest entry points can open auth in context."
  artifacts:
    - path: "src/navigation/rootNavigation.ts"
      provides: "shared helper for launching auth from guest surfaces in a consistent mode"
    - path: "src/screens/more/MoreScreen.tsx"
      provides: "benefit-led guest CTA for syncing progress"
    - path: "docs/release-smoke-checklist.md"
      provides: "manual verification guidance for guest-first onboarding and auth re-entry"
  key_links:
    - from: "src/screens/more/MoreScreen.tsx"
      to: "src/navigation/rootNavigation.ts"
      via: "guest CTA auth launch"
      pattern: "openAuthFlow"
    - from: "src/screens/more/ProfileScreen.tsx"
      to: "src/navigation/rootNavigation.ts"
      via: "guest profile auth launch"
      pattern: "openAuthFlow"
---

<objective>
Make deferred auth feel intentional by improving the guest entry points and verifying the new flow end to end.

Purpose: Removing auth from first run only helps if users can still discover authentication naturally when sync or account identity becomes useful. This plan upgrades the highest-signal guest prompts and records the new verification path.

Output: Better just-in-time auth entry points, preserved guest-safe behavior, and a release-smoke checklist that reflects the new first-run/auth expectations.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-CONTEXT.md
@.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-RESEARCH.md

@src/navigation/rootNavigation.ts
@src/screens/more/MoreScreen.tsx
@src/screens/more/ProfileScreen.tsx
@src/services/plans/readingPlanService.ts
@src/services/groups/groupService.ts
@src/screens/more/moreScreenSource.test.ts
@docs/release-smoke-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize the highest-signal guest CTAs around the new auth surface</name>
  <files>src/navigation/rootNavigation.ts, src/screens/more/MoreScreen.tsx, src/screens/more/ProfileScreen.tsx, src/screens/more/moreScreenSource.test.ts</files>
  <action>
    Update the main guest-facing surfaces so they use the unified auth route consistently and explain the benefit of auth in EveryBible terms (sync progress, save your data, continue across devices).

    Focus first on:
    - More tab guest CTA
    - Profile guest CTA/state

    Prefer the shared navigation helper over ad hoc `navigate('Auth')` calls where that improves consistency.
  </action>
  <verify>node --test --import tsx src/screens/more/moreScreenSource.test.ts</verify>
  <done>The primary guest surfaces now point into auth intentionally and use benefit-led copy.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the local-first vs account-bound boundary explicitly</name>
  <files>src/screens/learn/GroupListScreen.tsx, src/screens/learn/PrayerWallScreen.tsx, src/navigation/rootNavigation.ts</files>
  <action>
    Audit the highest-signal account-bound guest paths and make sure they do not silently strand users after onboarding is simplified.

    The intent is not to rework every service in the app. It is to make sure at least the most visible account-bound actions can route the user into auth intentionally, while local-first reading and browsing continue untouched.

    Preserve the existing guest-safe behavior of reading plans and other local-first flows.
  </action>
  <verify>npm run typecheck</verify>
  <done>The app has a clearer line between guest-safe use and account-bound prompts after auth is removed from first run.</done>
</task>

<task type="auto">
  <name>Task 3: Update the manual verification story for the new first-run/auth flow</name>
  <files>docs/release-smoke-checklist.md</files>
  <action>
    Update the release smoke checklist so it explicitly covers:
    - fresh install -> onboarding -> guest entry
    - More/Profile -> auth launch
    - email create-account success behavior
    - provider sign-in still dismissing correctly

    Keep the checklist short and operator-friendly.
  </action>
  <verify>rg -n "guest|onboarding|auth|provider|Profile|More" docs/release-smoke-checklist.md</verify>
  <done>Manual QA now reflects the guest-first onboarding and deferred-auth contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/screens/more/moreScreenSource.test.ts
- [ ] npx eslint src/navigation/rootNavigation.ts src/screens/more/MoreScreen.tsx src/screens/more/ProfileScreen.tsx src/screens/learn/GroupListScreen.tsx src/screens/learn/PrayerWallScreen.tsx
- [ ] npm run typecheck
- [ ] `docs/release-smoke-checklist.md` includes guest-first onboarding plus unified-auth re-entry checks
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Deferred auth is discoverable from the app's clearest guest surfaces
- SYNC-02 benefits from better auth timing without breaking local-first guest usage
</success_criteria>

<output>
After completion, create `.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-03-SUMMARY.md`
</output>
