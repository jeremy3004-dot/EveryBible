---
phase: 35-signup-simplification-and-deferred-auth-conversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App.tsx
  - src/screens/onboarding/localeSetupModel.ts
  - src/screens/onboarding/localeSetupModel.test.ts
  - src/screens/onboarding/LocaleSetupFlow.tsx
  - src/navigation/rootNavigation.ts
  - src/screens/onboarding/localeSetupFlowSource.test.ts
autonomous: true
requirements: [LOCL-01, AUTH-01]
user_setup: []
must_haves:
  truths:
    - "Initial onboarding asks only for interface language, country, and content language."
    - "Completing first-run onboarding lands the user in the app as a guest without auto-opening auth."
    - "The settings-mode locale flow remains intact and does not inherit first-run auth behavior."
  artifacts:
    - path: "src/screens/onboarding/localeSetupModel.ts"
      provides: "guest-first onboarding step contract for initial vs settings modes"
    - path: "App.tsx"
      provides: "first-run completion behavior without queued auth handoff"
    - path: "src/screens/onboarding/localeSetupFlowSource.test.ts"
      provides: "focused regression coverage for onboarding/auth handoff behavior"
  key_links:
    - from: "src/screens/onboarding/LocaleSetupFlow.tsx"
      to: "src/screens/onboarding/localeSetupModel.ts"
      via: "step ordering for initial onboarding"
      pattern: "getLocaleSetupSteps"
    - from: "App.tsx"
      to: "src/navigation/rootNavigation.ts"
      via: "initial auth handoff removal"
      pattern: "openAuthFlow"
---

<objective>
Remove the first-run auth detour and make onboarding honestly guest-first.

Purpose: The current flow asks users to choose an auth mode during onboarding, then routes them into auth later anyway. This plan removes that duplicate decision so people can finish locale setup and reach the app quickly.

Output: A simplified onboarding contract, updated first-run handoff behavior, and regression coverage that locks in the guest-first entry path.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-onboarding-and-preference-cohesion/02-RESEARCH.md
@.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-CONTEXT.md
@.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-RESEARCH.md

@App.tsx
@src/screens/onboarding/LocaleSetupFlow.tsx
@src/screens/onboarding/localeSetupModel.ts
@src/screens/onboarding/localeSetupModel.test.ts
@src/navigation/rootNavigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD the guest-first onboarding step contract</name>
  <files>src/screens/onboarding/localeSetupModel.ts, src/screens/onboarding/localeSetupModel.test.ts</files>
  <action>
    Update the initial onboarding model so the `account` step is no longer part of first run. Start with failing tests that prove:
    - `initial` mode now uses interface -> country -> contentLanguage
    - `settings` mode stays country -> contentLanguage
    - no auth-choice step is still expected by the model

    Keep the model small and explicit rather than scattering onboarding step logic through the screen component.
  </action>
  <verify>node --test --import tsx src/screens/onboarding/localeSetupModel.test.ts</verify>
  <done>The step model proves onboarding is now preference-first and no longer auth-first.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the queued auth handoff from first run</name>
  <files>App.tsx, src/screens/onboarding/LocaleSetupFlow.tsx, src/navigation/rootNavigation.ts</files>
  <action>
    Simplify the first-run completion contract so finishing locale setup does not pass an auth mode back to `App.tsx` and does not queue auth navigation in `rootNavigation.ts`.

    Preserve:
    - the settings-mode locale flow
    - existing privacy-lock behavior
    - the app shell's post-onboarding boot behavior

    Avoid replacing onboarding with a larger redesign. The goal is to remove the auth detour, not to re-theme or re-architect first run.
  </action>
  <verify>node --test --import tsx src/screens/onboarding/localeSetupModel.test.ts src/services/startup/startupBootSurface.test.ts</verify>
  <done>First-run onboarding completes directly into the app shell without auth queueing or mode handoff.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused source coverage for the new first-run contract</name>
  <files>App.tsx, src/screens/onboarding/LocaleSetupFlow.tsx, src/screens/onboarding/localeSetupFlowSource.test.ts</files>
  <action>
    Add a focused source-oriented test that protects the new contract:
    - LocaleSetupFlow no longer renders the auth-choice step in initial mode
    - App.tsx no longer routes initial onboarding completion through `accessMode`
    - first-run still marks onboarding complete before rendering the main shell

    Prefer small source assertions over broad UI harness work.
  </action>
  <verify>node --test --import tsx src/screens/onboarding/localeSetupModel.test.ts src/screens/onboarding/localeSetupFlowSource.test.ts</verify>
  <done>The repo has a small regression net that will catch the auth-detour coming back.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/screens/onboarding/localeSetupModel.test.ts src/screens/onboarding/localeSetupFlowSource.test.ts
- [ ] npx eslint App.tsx src/screens/onboarding/LocaleSetupFlow.tsx src/screens/onboarding/localeSetupModel.ts src/navigation/rootNavigation.ts
- [ ] The initial onboarding path no longer depends on `accessMode` or queued auth navigation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- New users can finish locale onboarding and enter the app as guests
- LOCL-01 is preserved while AUTH friction is reduced
</success_criteria>

<output>
After completion, create `.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-01-SUMMARY.md`
</output>
