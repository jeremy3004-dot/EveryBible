---
phase: 35-signup-simplification-and-deferred-auth-conversion
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - src/navigation/AuthStack.tsx
  - src/navigation/types.ts
  - src/screens/auth/AuthScreen.tsx
  - src/screens/auth/index.ts
  - src/screens/auth/authScreenSource.test.ts
  - src/services/auth/authService.ts
  - src/i18n/locales/en.ts
  - src/i18n/locales/es.ts
  - src/i18n/locales/ne.ts
  - src/i18n/locales/hi.ts
autonomous: true
requirements: [AUTH-01, AUTH-02]
user_setup: []
must_haves:
  truths:
    - "The app presents one shared auth surface instead of separate sign-in and sign-up shells."
    - "Apple and Google remain available on the unified surface where supported."
    - "Email account creation requires only email and password on the first auth step."
  artifacts:
    - path: "src/screens/auth/AuthScreen.tsx"
      provides: "shared auth shell with mode-aware email form and provider buttons"
    - path: "src/navigation/AuthStack.tsx"
      provides: "simplified auth navigation contract"
    - path: "src/screens/auth/authScreenSource.test.ts"
      provides: "regression coverage for session hydration and provider flow behavior on the unified screen"
  key_links:
    - from: "src/screens/auth/AuthScreen.tsx"
      to: "src/services/auth/authService.ts"
      via: "email/provider auth actions"
      pattern: "signInWithEmail|signUpWithEmail|signInWithApple|signInWithGoogle"
    - from: "src/navigation/AuthStack.tsx"
      to: "src/navigation/types.ts"
      via: "single auth route contract"
      pattern: "AuthStackParamList"
---

<objective>
Replace the split sign-in/sign-up UI with one unified auth surface and reduce the upfront email friction.

Purpose: The app currently duplicates most auth behavior across two separate screens and asks for more information than necessary during account creation. This plan simplifies the auth shell without changing the provider stack.

Output: A single auth screen, a leaner email create-account path, updated navigation/types, and focused regression coverage for shared auth behavior.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-CONTEXT.md
@.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-RESEARCH.md

@src/navigation/AuthStack.tsx
@src/navigation/types.ts
@src/screens/auth/SignInScreen.tsx
@src/screens/auth/SignUpScreen.tsx
@src/screens/auth/authScreenSource.test.ts
@src/services/auth/authService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared auth shell and simplify the route contract</name>
  <files>src/navigation/AuthStack.tsx, src/navigation/types.ts, src/screens/auth/AuthScreen.tsx, src/screens/auth/index.ts</files>
  <action>
    Replace the current split route setup with a shared auth screen that accepts an initial mode (`signIn` or `signUp`) but renders one shell, one provider section, and one shared close/back behavior.

    Keep the current More-stack modal presentation unless a simpler routing change is clearly better inside the existing architecture.

    The shared shell should own:
    - title/subtitle by mode
    - provider buttons
    - email form layout
    - mode switch CTA
    - session hydration after successful auth
  </action>
  <verify>node --test --import tsx src/screens/auth/authScreenSource.test.ts</verify>
  <done>The auth experience is structurally unified and no longer split into two separate screen shells.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce email create-account friction without changing provider behavior</name>
  <files>src/screens/auth/AuthScreen.tsx, src/services/auth/authService.ts</files>
  <action>
    Simplify the create-account mode so the initial email path asks only for `email` and `password`.

    Preserve:
    - the current service boundary in `authService`
    - provider auth behavior and session hydration
    - translated validation and failure messaging

    Remove or avoid:
    - initial `displayName` collection
    - `confirmPassword` as a required first-step field

    If account creation still requires email verification, show a calmer in-flow success state rather than the current abrupt alert whenever that can be done without backend changes.
  </action>
  <verify>node --test --import tsx src/screens/auth/authScreenSource.test.ts src/services/auth/authErrors.test.ts</verify>
  <done>The first-step email create-account path is materially lighter while the provider stack stays intact.</done>
</task>

<task type="auto">
  <name>Task 3: Update translation keys and regression coverage for the new auth screen</name>
  <files>src/screens/auth/authScreenSource.test.ts, src/i18n/locales/en.ts, src/i18n/locales/es.ts, src/i18n/locales/ne.ts, src/i18n/locales/hi.ts</files>
  <action>
    Add only the translation keys that the unified auth surface genuinely needs, mirror them across the required locale files, and update source-based tests so they assert the new shared screen still:
    - hydrates a live session after successful auth
    - keeps provider flows available
    - switches modes without relying on two duplicated screen files
  </action>
  <verify>node --test --import tsx src/screens/auth/authScreenSource.test.ts && npm run typecheck</verify>
  <done>The unified auth surface is fully translatable and covered by focused tests rather than assumption.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/screens/auth/authScreenSource.test.ts src/services/auth/authErrors.test.ts
- [ ] npx eslint src/navigation/AuthStack.tsx src/navigation/types.ts src/screens/auth/AuthScreen.tsx src/screens/auth/index.ts src/services/auth/authService.ts
- [ ] npm run typecheck
- [ ] The app still supports Apple and Google auth while presenting one shared auth shell
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users see one auth screen instead of two parallel auth shells
- AUTH-01 and AUTH-02 are preserved while the create-account path gets lighter
</success_criteria>

<output>
After completion, create `.planning/phases/35-signup-simplification-and-deferred-auth-conversion/35-02-SUMMARY.md`
</output>
