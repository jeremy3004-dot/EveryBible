# Plan 01 Summary

## Outcome

EveryBible now has a real audio-only translation path for no-text testing and usage. Users can select an audio-only translation when audio is actually playable (remote or offline), and both reader and daily-scripture presentation logic now explicitly handle the edge case where text is missing and no audio source is available.

## Changes

- Added `src/screens/bible/bibleTranslationModel.ts` and `src/screens/bible/bibleTranslationModel.test.ts` to centralize translation selection eligibility, including the audio-only/no-audio fallback rule.
- Added an audio-only translation profile in `src/constants/translations.ts` (`bsb_audio`) with chapter-based audio metadata and no text.
- Updated `src/screens/bible/BibleBrowserScreen.tsx` to use selection-state rules for translation picking and surface clearer availability states for selectable audio-only translations.
- Updated `src/screens/bible/BibleReaderScreen.tsx` to apply the same selection-state rules in reader translation switching and provide explicit feedback when audio cannot currently play.
- Updated `src/stores/bibleStore.ts` so store-level translation switching allows audio-only translations only when runtime audio capability exists.
- Expanded no-text fallback coverage in `src/services/bible/presentation.test.ts` and `src/services/bible/dailyScripture.test.ts` to assert both:
  - audio available -> audio-first/section audio behavior
  - audio unavailable -> empty fallback behavior

## 2026-03-19 Follow-up

- Replaced the temporary `bsb_audio` placeholder with a real `web` audio-only translation backed by public-domain eBible.org chapter MP3s, so the app now exposes at least one downloadable English audio Bible without requiring `EXPO_PUBLIC_BIBLE_IS_API_KEY`.
- Added `src/services/audio/audioRemote.test.ts` to lock direct public-audio URL resolution for both standard chapters and Psalms, and tightened `src/stores/persistedStateSanitizers.test.ts` so existing installs upgrade into the new audio-capable WEB metadata instead of keeping stale persisted capabilities.
- Refreshed `src/components/audio/AudioFirstChapterCard.tsx` into a book-art hero treatment for no-text listening chapters and removed stale locale copy that incorrectly said only BSB was available.

## Verification

- `node --test --import tsx src/screens/bible/bibleTranslationModel.test.ts src/services/bible/presentation.test.ts src/services/bible/dailyScripture.test.ts src/services/audio/audioAvailability.test.ts src/services/audio/audioDownloadService.test.ts src/stores/persistedStateSanitizers.test.ts`
- `npm test`
- `npx eslint src/constants/translations.ts src/screens/bible/BibleBrowserScreen.tsx src/screens/bible/BibleReaderScreen.tsx src/screens/bible/bibleTranslationModel.ts src/screens/bible/bibleTranslationModel.test.ts src/services/bible/presentation.test.ts src/services/bible/dailyScripture.test.ts src/stores/bibleStore.ts src/stores/persistedStateSanitizers.ts`

## Remaining Manual Checks

- On device, select `WEB`, download one book and full translation audio, and confirm chapter playback works.
- Disable network after download and confirm offline playback still works for downloaded WEB chapters.
- Validate no-text chapter behavior on device: audio-first card when audio is available, and clear empty fallback when audio is unavailable.
