# Summary 12.1-01

- Locked the premium reader target to the supplied video instead of the earlier screenshot.
- Added a dedicated reader-motion contract in `bibleReaderModel.ts` with tested collapse thresholds and clamped progress helpers.
- Extended source-level regression coverage so the screen now has to use animated scroll-driven chrome, blur-backed surfaces, and the dedicated collapsed chapter pill.

## Verification

- `node --test --import tsx src/screens/bible/bibleReaderModel.test.ts src/screens/bible/bibleReaderChromeSource.test.ts`

## Outcome

The premium reader redesign now has explicit motion rules and automated guardrails instead of relying on hand-tuned UI guesses.
