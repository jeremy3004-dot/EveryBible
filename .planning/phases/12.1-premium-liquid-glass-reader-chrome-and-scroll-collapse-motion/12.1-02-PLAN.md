# Plan 12.1-02: Rebuild The Read-Mode Reader Chrome

## Objective

Replace the current read-mode Bible reader shell with the overlay-based glass chrome and collapse behavior from the supplied video.

## Scope

- Add any missing blur dependency needed for glass-backed surfaces.
- Rebuild the read-mode `BibleReaderScreen` layout around animated top and bottom overlays.
- Move read-mode translation/font controls into the bottom floating chrome and remove the old static read header/audio bar treatment.
- Apply reader-specific editorial typography that matches the video more closely than the current sans treatment.

## Out Of Scope

- Reworking the dedicated listen-mode transport layout
- Changing chapter loading, playback service contracts, or navigation routes
- Retheming the wider app outside the reader-specific surface

## Verification

```bash
node --test --import tsx src/screens/bible/bibleReaderModel.test.ts src/screens/bible/bibleReaderChromeSource.test.ts src/design/designSystemSource.test.ts
npx eslint src/screens/bible/BibleReaderScreen.tsx src/screens/bible/bibleReaderModel.ts src/screens/bible/bibleReaderChromeSource.test.ts src/design/system.ts
npm run typecheck
```
