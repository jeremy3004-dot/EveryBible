# Summary 12.1-02

- Rebuilt `BibleReaderScreen` read mode as an overlay-first canvas with blur-backed glass controls, a centered serif chapter hero, and floating bottom controls that collapse down to a compact chapter pill.
- Removed the old static read-mode header and bottom `AudioPlayerBar` from the premium read surface.
- Added reader-specific typography tokens in `src/design/system.ts` and installed `expo-blur` to support the liquid-glass treatment.

## Verification

- `node --test --import tsx src/screens/bible/bibleReaderModel.test.ts src/screens/bible/bibleReaderChromeSource.test.ts`
- `npx eslint src/screens/bible/BibleReaderScreen.tsx src/screens/bible/bibleReaderModel.ts src/design/system.ts`
- `npm run typecheck`

## Outcome

The read-mode Bible experience now follows the supplied video’s resting layout and collapse choreography while preserving the existing chapter-session model.
