# Plan 12.1-03: Lock The Reader Redesign For Release

## Objective

Prove the premium reader redesign is stable enough to ship by extending regression coverage, running the release checks, and preparing a new TestFlight build.

## Scope

- Extend source-level assertions for the new read-mode control placement and removal of the old read audio bar.
- Run lint, typecheck, targeted tests, full `npm test`, and release precheck commands.
- If verification is green, package and submit the next local iOS build to TestFlight and confirm tester visibility.

## Out Of Scope

- Additional redesign work beyond issues caught during verification
- New product scope unrelated to the premium reader phase

## Verification

```bash
node --test --import tsx src/screens/bible/bibleReaderModel.test.ts src/screens/bible/bibleReaderChromeSource.test.ts src/design/designSystemSource.test.ts
npm test
npm run lint
npm run typecheck
npm run release:verify
```
