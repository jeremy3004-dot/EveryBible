---
phase: 01-startup-and-backend-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/services/sync/syncService.ts
  - src/services/sync/syncMerge.ts
  - src/services/supabase/types.ts
  - src/stores/persistedStateSanitizers.ts
  - src/stores/persistedStateSanitizers.test.ts
  - src/services/sync/syncMerge.test.ts
  - app.json
  - .env.example
  - src/services/startup/runtimeConfig.test.ts
  - android/gradle.properties
  - android/app/src/main/AndroidManifest.xml
  - ios/Podfile.properties.json
  - ios/EveryBible/Info.plist
  - supabase/config.toml
  - supabase/schema.sql
  - supabase/migrations/20240101000000_initial_schema.sql
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup:
  - OAuth provider credentials and Supabase redirect settings must exist in the target environment for final release-path verification.
must_haves:
  truths:
    - Reading progress and preferences merge predictably between local and remote state on reconnect and foreground transitions.
    - Native and Expo configuration required for auth callbacks and startup architecture are aligned across platforms.
    - Supabase auth and sync assumptions used by the app are explicit enough that signup and sync do not depend on undocumented backend state.
    - Phase 1 ends with a documented verification path for config-sensitive release behavior, not only unit-level confidence.
  artifacts:
    - Sync logic and its regression tests reflect the intended offline-first merge rules.
    - Auth- and startup-relevant config files are intentionally aligned or intentionally documented if divergent.
    - Supabase-facing signup and sync contracts are checked against repo schema/config artifacts.
    - The repo carries a clear verification record for config-sensitive behaviors.
  key_links:
    - useSync depends on auth initialization and merge helpers that are covered by tests.
    - Deep link, provider, and architecture settings agree across tracked Expo config and derived native platform files.
    - Phase verification uses both automated sync tests and manual release-path checks.
---

<objective>
Validate and harden sync semantics, Supabase-facing contracts, and native configuration dependencies so authenticated users keep correct local-first behavior across reconnects, restarts, and platform builds.

Purpose: Startup/auth reliability is incomplete if sync semantics or config drift can still break user state after launch.
Output: Updated sync/config modules, stronger tests around merge behavior, and aligned platform configuration for the current release path.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-startup-and-backend-hardening/01-RESEARCH.md
@.planning/phases/01-startup-and-backend-hardening/01-VALIDATION.md

@src/services/sync/syncService.ts
@src/services/sync/syncMerge.ts
@src/stores/persistedStateSanitizers.ts
@src/services/supabase/types.ts
@app.json
@android/gradle.properties
@android/app/src/main/AndroidManifest.xml
@ios/Podfile.properties.json
@ios/EveryBible/Info.plist
@supabase/config.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden sync merge rules and authenticated sync orchestration</name>
  <files>src/services/sync/syncService.ts, src/services/sync/syncMerge.ts, src/stores/persistedStateSanitizers.ts, src/services/sync/syncMerge.test.ts, src/stores/persistedStateSanitizers.test.ts</files>
  <action>Audit the current local-first merge behavior for reading progress and preferences, then tighten any gaps that could cause stale remote data, duplicate writes, onboarding regressions, or incorrect preference restoration. Keep the merge-helper pattern centralized in syncMerge, and make sure sync orchestration remains gated by authenticated, initialized state instead of caller assumptions.</action>
  <verify>node --test --import tsx src/services/sync/syncMerge.test.ts src/stores/persistedStateSanitizers.test.ts src/services/startup/startupService.test.ts</verify>
  <done>Sync merge rules are explicit, offline-first behavior remains intact, and targeted sync-related tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Align Expo, native, and Supabase contracts for startup/auth reliability</name>
  <files>app.json, .env.example, src/services/startup/runtimeConfig.test.ts, android/gradle.properties, android/app/src/main/AndroidManifest.xml, ios/Podfile.properties.json, ios/EveryBible/Info.plist, supabase/config.toml, supabase/schema.sql, supabase/migrations/20240101000000_initial_schema.sql, src/services/supabase/types.ts</files>
  <action>Review and align the config surface that affects startup, auth callbacks, architecture mode, and the backend assumptions the app already makes. Resolve or explicitly document mismatches such as New Architecture flags, supported Google client ID inputs, auth URL scheme behavior, redirect expectations, enabled email signup, and schema/type expectations for auth profile creation and sync tables. Treat `/ios` and `/android` as derived local artifacts because this repo ignores them; every durable config change should live in tracked source files.</action>
  <verify>node --test --import tsx src/services/startup/runtimeConfig.test.ts && npx expo config --type public && rg -n "enable_signup = true|handle_new_user|profiles" supabase/config.toml supabase/schema.sql supabase/migrations/20240101000000_initial_schema.sql</verify>
  <done>Tracked startup/auth config is aligned, the repo-level Supabase contracts used by signup and sync are explicitly verified, and derived native output is documented as a generated artifact.</done>
</task>

<task type="auto">
  <name>Task 3: Capture verification evidence for release-path startup, auth, and sync behavior</name>
  <files>app.json, android/gradle.properties, android/app/src/main/AndroidManifest.xml, ios/Podfile.properties.json, ios/EveryBible/Info.plist, supabase/config.toml</files>
  <action>Translate the Phase 1 validation strategy into concrete execution evidence: run the automated sync tests, confirm config alignment in code, and record any remaining manual-only checks that still require release-like device validation. If a behavior cannot be fully automated in the current environment, document it clearly in the eventual summary so Phase 1 completion does not overclaim certainty.</action>
  <verify>npm test</verify>
  <done>Phase 1 ends with explicit evidence for what was automated, what was manually checked, and what remains environment-dependent.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node --test --import tsx src/services/sync/syncMerge.test.ts src/stores/persistedStateSanitizers.test.ts
- [ ] npx expo config --type public
- [ ] npm test
- [ ] Config-sensitive release-path checks are documented with no hidden assumptions
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SYNC-01 and SYNC-02 are materially advanced with stronger merge and orchestration confidence
- Auth/startup config dependencies are aligned closely enough that later release work has a stable base
</success_criteria>

<output>
After completion, create `.planning/phases/01-startup-and-backend-hardening/01-02-SUMMARY.md`
</output>
