---
phase: 10-admin-codex-operator-for-guided-site-and-data-changes
plan: "03"
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - .env.example
  - config/openclaw/everybible-gateway.example.json
  - docs/openclaw/everybible-operator-setup.md
  - scripts/openclaw/bootstrap-local-host.sh
  - scripts/openclaw/verify-local-host.sh
  - apps/site/app/layout.tsx
  - apps/site/app/globals.css
  - apps/site/components/OperatorLauncher.tsx
  - apps/site/lib/operator-launcher.ts
  - apps/site/lib/operator-launcher.test.ts
autonomous: false
requirements:
  - COPILOT-01
  - COPILOT-02
  - COPILOT-03
user_setup:
  - service: telegram
    why: "First-run pairing and steady-state allowlist verification for the real EveryBible operator bot"
    env_vars:
      - name: EVERYBIBLE_OPERATOR_TELEGRAM_BOT_TOKEN
        source: "Telegram BotFather -> bot token"
      - name: EVERYBIBLE_OPERATOR_TELEGRAM_ALLOW_FROM
        source: "Approved Telegram sender ID captured from pairing and stored as tg:<numeric-id>"
    dashboard_config:
      - task: "Send the first DM from the real approved Telegram account while pairing mode is enabled, then confirm allowlist mode after approval"
        location: "Telegram DM with the EveryBible operator bot"
  - service: honcho
    why: "Only if the Honcho decision gate chooses service-backed memory beyond the bundled memory-core baseline"
    env_vars:
      - name: HONCHO_BASE_URL
        source: "Honcho managed endpoint or self-hosted base URL"
      - name: HONCHO_API_KEY
        source: "Honcho dashboard API key when using managed Honcho"
    dashboard_config:
      - task: "Run the Honcho setup flow only after baseline Telegram + memory-core verification passes"
        location: "Local OpenClaw host CLI"
must_haves:
  truths:
    - "The local machine can bring up the real EveryBible OpenClaw gateway with the pinned Telegram, ACP, and EveryBible plugin configuration without committing secrets."
    - "Pairing is a one-time bootstrap step and steady-state access returns to an explicit Telegram allowlist before the operator is considered live."
    - "The chosen memory mode is explicit: the bundled memory-core baseline is enough for go-live on a single trusted host, and Honcho is enabled only after a deliberate decision."
    - "Every public `apps/site` page shows a bottom-right launcher that opens the configured operator chat entrypoint without pretending the public site has unrestricted production authority."
  artifacts:
    - path: "scripts/openclaw/bootstrap-local-host.sh"
      provides: "Safe, repeatable local host bootstrap commands for OpenClaw, the EveryBible plugin, and Telegram pairing mode"
    - path: "scripts/openclaw/verify-local-host.sh"
      provides: "Pinned health and verification checks for Telegram, ACP, plugin status, and allowlist readiness"
    - path: "docs/openclaw/everybible-operator-setup.md"
      provides: "Updated host bring-up runbook including env bootstrap, pairing-to-allowlist flow, and optional Honcho branch"
    - path: "apps/site/components/OperatorLauncher.tsx"
      provides: "Persistent floating launcher UI rendered across the public site"
    - path: "apps/site/lib/operator-launcher.ts"
      provides: "Validated launcher config/model seam so the site can open the approved operator chat target safely"
  key_links:
    - from: "scripts/openclaw/bootstrap-local-host.sh"
      to: "config/openclaw/everybible-gateway.example.json"
      via: "copy/validate pinned local gateway config before startup"
      pattern: "everybible-gateway\\.example\\.json|dmPolicy|allowFrom"
    - from: "scripts/openclaw/verify-local-host.sh"
      to: "OpenClaw runtime"
      via: "doctor/health/status/plugins/pairing checks"
      pattern: "openclaw (doctor|health|status|plugins|pairing)"
    - from: "apps/site/app/layout.tsx"
      to: "apps/site/components/OperatorLauncher.tsx"
      via: "global public-site render path"
      pattern: "OperatorLauncher"
    - from: "apps/site/components/OperatorLauncher.tsx"
      to: "apps/site/lib/operator-launcher.ts"
      via: "env-driven launcher model and safe target resolution"
      pattern: "getOperatorLauncherConfig|buildOperatorLauncherModel"
---

<objective>
Finish the last-mile Phase 10 work that was intentionally left outside the repo-only scope of 10-01 and 10-02: real host activation for the Telegram operator and a public-site launcher that opens the operator chat.

Purpose: Turn the already-built OpenClaw package and homepage tools into a locally verifiable operator that Jeremy can actually use, while giving the public site a clear but still guarded entrypoint into that operator experience.
Output: Local host bootstrap/verification scripts and runbook updates, an explicit Honcho decision gate, and a persistent bottom-right launcher rendered across `apps/site`.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-platform/PROJECT.md
@.planning/workstreams/web-platform/ROADMAP.md
@.planning/workstreams/web-platform/STATE.md
@.planning/workstreams/web-platform/REQUIREMENTS.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-CONTEXT.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-RESEARCH.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-01-SUMMARY.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-02-SUMMARY.md
@docs/plans/2026-04-02-everybible-codex-operator-design.md
@docs/openclaw/everybible-operator-setup.md
@config/openclaw/everybible-gateway.example.json
@packages/openclaw-everybible/src/index.ts
@packages/openclaw-everybible/src/tools/homepage.ts
@packages/openclaw-everybible/src/tools/code-change-request.ts
@apps/site/app/layout.tsx
@apps/site/app/globals.css
@apps/site/lib/env.ts

<interfaces>
From packages/openclaw-everybible/src/index.ts:
```ts
api.registerTool((context) => createGetHomepageContentTool(context));
api.registerTool((context) => createGetContentHealthSummaryTool(context));
api.registerTool((context) => createGetTranslationSummaryTool(context));
api.registerTool((context) => createListRecentAdminActionsTool(context));
api.registerTool((context) => createUpdateHomepageContentTool(context), {
  optional: true,
  name: 'update_homepage_content',
});
api.registerTool((context) => createRequestCodeChangeTool(context), {
  optional: true,
  name: 'request_code_change',
});
```

From config/openclaw/everybible-gateway.example.json:
```json5
channels: {
  telegram: {
    enabled: true,
    botToken: "${EVERYBIBLE_OPERATOR_TELEGRAM_BOT_TOKEN}",
    dmPolicy: "allowlist",
    allowFrom: ["${EVERYBIBLE_OPERATOR_TELEGRAM_ALLOW_FROM}"],
    groupPolicy: "disabled",
  },
}
```

From apps/site/app/layout.tsx:
```tsx
export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <head>{/* global styles + metadata */}</head>
      <body>{children}</body>
    </html>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add safe local-host bootstrap and verification assets for the real Telegram operator</name>
  <read_first>
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-RESEARCH.md
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-02-SUMMARY.md
    - docs/openclaw/everybible-operator-setup.md
    - config/openclaw/everybible-gateway.example.json
    - .env.example
    - packages/openclaw-everybible/src/index.ts
  </read_first>
  <files>.env.example, config/openclaw/everybible-gateway.example.json, docs/openclaw/everybible-operator-setup.md, scripts/openclaw/bootstrap-local-host.sh, scripts/openclaw/verify-local-host.sh</files>
  <action>Create a repo-owned local-host bring-up path that closes the "host-side setup still required" gap left in 10-02. Add two shell scripts: one for safe bootstrap and one for verification. The bootstrap script should stay idempotent and secret-safe: verify `openclaw@2026.4.1`, ensure `acpx` and the local EveryBible plugin are installed from this workspace, copy or validate the pinned gateway config, and support an explicit pairing bootstrap mode before steady-state allowlist mode. The verification script should run the pinned health loop from the runbook (`openclaw doctor`, `openclaw health`, `openclaw status`, `openclaw plugins status`, plus Telegram pairing/allowlist checks) and print the exact next manual DM step when host-side confirmation is still required. Update `.env.example`, the example gateway config, and the runbook so they describe the real local-machine path, token/bootstrap expectations, and pairing-to-allowlist flow clearly. Per the locked Phase 10 decisions, do not add raw repo-write authority, raw SQL authority, or a fake "fully automatic" host setup that hides manual Telegram approval.</action>
  <verify>
    <automated>bash -n scripts/openclaw/bootstrap-local-host.sh scripts/openclaw/verify-local-host.sh && npm run operator:typecheck && npm run operator:test</automated>
  </verify>
  <acceptance_criteria>
    - The repo contains a concrete local-host bootstrap path for the actual EveryBible Telegram bot rather than documentation-only placeholders.
    - Pairing and allowlist are both documented and script-supported as separate states, with allowlist restored before go-live.
    - Verification commands are centralized and repeatable without committing secrets or modifying unrelated host services.
  </acceptance_criteria>
  <done>The local machine can be brought to a verified "ready for Telegram DM" state using repo-owned scripts and docs, with pairing as a temporary bootstrap and allowlist as the steady-state guardrail.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Decide whether to enable Honcho beyond the default memory-core setup</name>
  <decision>Choose the Phase 10 memory mode after baseline Telegram + memory-core verification passes</decision>
  <context>The live OpenClaw `2026.4.1` host setup shows that `memory-lancedb` now requires an embeddings API key, so the zero-config baseline for this phase is the bundled `memory-core` path. Honcho is still optional and should only be enabled if the operator needs service-backed cross-session memory beyond a single trusted host.</context>
  <action>After Task 1 reaches a verified Telegram-ready state, compare the observed baseline memory behavior against the real need for richer service-backed recall. If `option-a` is selected, keep `memory-core` as the enabled memory path and leave Honcho disabled. If `option-b` is selected, continue execution with the Honcho branch in the runbook, add the required secrets on the local host, and rerun the memory smoke test before claiming go-live readiness.</action>
  <options>
    <option id="option-a">
      <name>Keep memory-core only for Phase 10 go-live</name>
      <pros>Simplest local bring-up, no extra API key, fewer secrets, lower operational risk</pros>
      <cons>Memory remains tied to the local OpenClaw host instead of a richer service-backed layer</cons>
    </option>
    <option id="option-b">
      <name>Enable Honcho now after baseline verification</name>
      <pros>Richer cross-session memory and a clearer path to multi-host continuity</pros>
      <cons>Extra setup, extra secrets, more moving pieces to debug during first go-live</cons>
    </option>
  </options>
  <verify>
    <automated>printf 'Record Honcho decision as option-a or option-b before continuing\n'</automated>
  </verify>
  <done>The selected memory mode is explicit, recorded during execution, and the remaining host-verification steps follow the chosen branch.</done>
  <resume-signal>Select: option-a or option-b</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the persistent bottom-right public-site launcher for the operator chat</name>
  <read_first>
    - apps/site/app/layout.tsx
    - apps/site/app/globals.css
    - apps/site/lib/env.ts
    - docs/plans/2026-04-02-everybible-codex-operator-design.md
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-CONTEXT.md
  </read_first>
  <files>.env.example, apps/site/app/layout.tsx, apps/site/app/globals.css, apps/site/components/OperatorLauncher.tsx, apps/site/lib/operator-launcher.ts, apps/site/lib/operator-launcher.test.ts</files>
  <behavior>
    - Test 1: when no public operator chat URL is configured, the launcher model resolves to disabled and the site renders no floating AI control.
    - Test 2: when a valid operator chat target is configured, the launcher model exposes a branded label, support copy, and a safe destination for the real operator chat entrypoint.
    - Test 3: the root site layout renders the launcher globally so it persists across marketing/legal/support pages and does not rely on homepage-only wiring.
  </behavior>
  <action>Add a small, branded floating launcher for `apps/site` that stays anchored at the bottom-right of every public page and opens the configured operator chat entrypoint. Prefer an env-driven target such as `NEXT_PUBLIC_EVERYBIBLE_OPERATOR_CHAT_URL` plus a pure helper that validates/normalizes the public chat destination before any UI renders. Implement the UI as a focused site component, not a large embedded chat system: when opened, it should present a short "guided operator" explanation and a primary action that opens the configured chat target (for example Telegram DM or a future hosted operator route). Keep the public-site boundary honest per the deferred ideas from Phase 10: do not embed unrestricted anonymous mutation tools, raw admin actions, or a fake public super-agent inside the marketing site. If the target is missing or invalid, render nothing rather than a broken affordance.</action>
  <verify>
    <automated>node --test --import tsx apps/site/lib/operator-launcher.test.ts && npm run site:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - A persistent bottom-right launcher exists across the public site and matches the existing site visual language.
    - The launcher opens the approved operator chat target instead of creating an unrestricted anonymous agent surface inside `apps/site`.
    - The launcher config path is test-covered and safely disables itself when the chat target is not configured.
  </acceptance_criteria>
  <done>`apps/site` exposes a global floating launcher that can open the operator chat for users while preserving the locked Phase 10 trust boundary.</done>
</task>

</tasks>

<verification>
Run the repo-side checks first:

```bash
bash -n scripts/openclaw/bootstrap-local-host.sh scripts/openclaw/verify-local-host.sh
npm run operator:typecheck
npm run operator:test
node --test --import tsx apps/site/lib/operator-launcher.test.ts
npm run site:typecheck
```

Then run the live host checks on the local machine:

1. Export the real Telegram/OpenAI env vars outside git and run `scripts/openclaw/bootstrap-local-host.sh --pairing`.
2. Send the real Telegram bot a DM, approve pairing with `openclaw pairing list telegram` and `openclaw pairing approve telegram <CODE>`, then switch back to allowlist mode and rerun `scripts/openclaw/verify-local-host.sh`.
3. Confirm `/acp status` reports `acpx` with `codex` as the default agent.
4. Visit the public site, confirm the floating launcher is visible in the bottom-right, click it, and verify it opens the configured operator chat entrypoint without overlapping critical mobile CTAs.
</verification>

<success_criteria>
The EveryBible operator can be brought up safely on the real local host with verified Telegram pairing/allowlist behavior, the memory mode is an explicit choice rather than an accidental default, and the public site exposes a persistent launcher that opens the approved operator chat entrypoint without widening Phase 10 into anonymous unrestricted AI access.
</success_criteria>

<output>
After completion, create `.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-03-SUMMARY.md`.
</output>
