---
phase: 10-admin-codex-operator-for-guided-site-and-data-changes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/index.ts
  - supabase/migrations/20260402210000_create_homepage_content_operator_contract.sql
  - apps/admin/lib/audit-log.ts
  - apps/admin/lib/admin-data.ts
  - apps/admin/app/(dashboard)/settings/page.tsx
  - apps/site/lib/site-content.ts
  - apps/site/lib/homepage-content.ts
  - apps/site/lib/homepage-content.test.ts
  - apps/site/app/page.tsx
autonomous: true
requirements:
  - COPILOT-03
  - COPILOT-04
  - COPILOT-05
must_haves:
  truths:
    - "The public homepage renders a valid live override when one is published and otherwise preserves the existing code-managed fallback."
    - "Operator-approved content mutations flow through explicit server-side contracts rather than raw SQL or prompt-defined JSON writes."
    - "Operator-triggered changes leave audit evidence that includes actor source, action, target, and changed fields."
    - "Admin settings exposes recent operator actions without introducing a separate admin chat surface."
  artifacts:
    - path: "supabase/migrations/20260402210000_create_homepage_content_operator_contract.sql"
      provides: "Homepage override table/RPC plus operator-audit metadata support"
    - path: "apps/site/lib/homepage-content.ts"
      provides: "Server-side homepage override resolver with validation and fallback"
    - path: "apps/admin/lib/audit-log.ts"
      provides: "Backward-compatible audit writer that can record operator metadata"
    - path: "apps/admin/app/(dashboard)/settings/page.tsx"
      provides: "Minimal operator audit visibility surface"
  key_links:
    - from: "apps/site/lib/homepage-content.ts"
      to: "public.get_live_homepage_content"
      via: "createSiteServiceClient RPC call"
      pattern: "rpc\\('get_live_homepage_content'"
    - from: "apps/admin/lib/audit-log.ts"
      to: "public.admin_audit_logs"
      via: "service-role insert"
      pattern: "from\\('admin_audit_logs'\\)\\.insert"
    - from: "apps/site/app/page.tsx"
      to: "apps/site/lib/homepage-content.ts"
      via: "server-side content resolution before render"
      pattern: "getHomepageContent"
---

<objective>
Add the shared Phase 10 data contract: a Supabase-backed homepage override surface with deterministic fallback, operator-grade audit metadata, and a minimal admin visibility seam.

Purpose: Deliver the first safe "change the site from the site" path before the OpenClaw package is introduced.
Output: New homepage content schema/RPC, typed homepage resolver + tests, upgraded audit sink, and a small operator audit feed in admin settings.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-platform/PROJECT.md
@.planning/workstreams/web-platform/ROADMAP.md
@.planning/workstreams/web-platform/STATE.md
@.planning/workstreams/web-platform/REQUIREMENTS.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-CONTEXT.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-RESEARCH.md
@docs/plans/2026-04-02-everybible-codex-operator-design.md
@apps/site/lib/site-content.ts
@apps/site/app/page.tsx
@apps/admin/lib/audit-log.ts
@apps/admin/lib/admin-data.ts
@apps/admin/app/(dashboard)/settings/page.tsx
@supabase/migrations/20260401130000_create_web_admin_platform.sql
@supabase/migrations/20260402091500_unify_shared_backend_contracts.sql
@packages/types/src/index.ts

<interfaces>
From apps/admin/lib/audit-log.ts:
```ts
interface AuditLogInput {
  action: string;
  actorEmail: string;
  actorUserId: string;
  entityId?: string | null;
  entityType: string;
  metadata?: Record<string, unknown>;
  summary: string;
}
```

From apps/site/lib/site-content.ts:
```ts
export interface HeroContent {
  title: string;
  description: string;
  visual: { src: string; alt: string };
  storeLinks: Array<{
    label: string;
    href: string;
    eyebrow: string;
    platform: 'google-play' | 'app-store';
  }>;
  inlineLink: { label: string; href: string };
}
```

From packages/types/src/index.ts:
```ts
export interface MobileContentOverridePayload {
  generatedAt: string;
  verseOfDay: { ... } | null;
  images: Array<{ ... }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the homepage override and operator-audit contracts</name>
  <read_first>
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-CONTEXT.md
    - docs/plans/2026-04-02-everybible-codex-operator-design.md
    - apps/admin/lib/audit-log.ts
    - supabase/migrations/20260401130000_create_web_admin_platform.sql
    - supabase/migrations/20260402091500_unify_shared_backend_contracts.sql
    - packages/types/src/index.ts
  </read_first>
  <files>packages/types/src/index.ts, supabase/migrations/20260402210000_create_homepage_content_operator_contract.sql, apps/admin/lib/audit-log.ts</files>
  <action>Extend the shared type package with a typed homepage payload contract and operator audit metadata shape that later tools can consume. Create a new migration that adds a single-purpose homepage content table or slugged content entry contract, plus a `get_live_homepage_content` security-definer read function that mirrors the existing RPC style from Phase 8/Phase 5. Reuse `admin_audit_logs` rather than creating a second audit table, but make the write helper accept backward-compatible metadata fields for operator source, channel, tool name, target slug, and changed field names. Per the locked Phase 10 decisions, do not introduce arbitrary JSON mutation endpoints, arbitrary SQL helpers, or a generic CMS schema.</action>
  <verify>
    <automated>npm run admin:typecheck && npm run site:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Shared types describe the homepage override payload and operator audit metadata without using `any`.
    - The new migration creates a single explicit homepage override contract and a read RPC, not a broad dynamic content system.
    - The audit helper can record operator metadata while remaining compatible with existing admin mutation callers.
  </acceptance_criteria>
  <done>Homepage override storage/RPC and operator audit metadata are defined in code and schema, and the existing admin audit sink is still callable by current Phase 4-9 flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement server-side homepage resolution with deterministic fallback</name>
  <read_first>
    - apps/site/lib/site-content.ts
    - apps/site/app/page.tsx
    - apps/site/app/api/mobile/content/route.ts
    - .planning/workstreams/web-platform/phases/02-public-marketing-site-v1/02A-SUMMARY.md
    - .planning/workstreams/web-platform/phases/02-public-marketing-site-v1/02B-SUMMARY.md
  </read_first>
  <files>apps/site/lib/site-content.ts, apps/site/lib/homepage-content.ts, apps/site/lib/homepage-content.test.ts, apps/site/app/page.tsx</files>
  <behavior>
    - Test 1: when the homepage RPC returns `null` or throws, the resolver returns the current code-managed homepage sections unchanged.
    - Test 2: when the RPC returns a valid live payload, the resolver normalizes and returns the remote homepage content.
    - Test 3: when the RPC returns an invalid or partial payload, invalid sections are rejected and the fallback content remains deterministic.
  </behavior>
  <action>Create a dedicated server-only homepage resolver that fetches the live override via the new RPC, validates it against the typed contract from Task 1, and returns the current `site-content.ts` values when the payload is unset or malformed. Keep `site-content.ts` as the canonical fallback source rather than moving the content into the database. Update `apps/site/app/page.tsx` to use the resolved content object during SSR and keep the existing visual structure intact. Add regression tests for no override, valid override, and invalid payload fallback; do not turn the homepage into a client fetch or introduce hidden caching behavior.</action>
  <verify>
    <automated>node --test --import tsx apps/site/lib/homepage-content.test.ts && npm run site:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The homepage still renders from static fallback content when remote content is absent or invalid.
    - A valid live override can change approved homepage sections without modifying source code.
    - The resolver is server-only and has deterministic tests covering fallback behavior.
  </acceptance_criteria>
  <done>`apps/site` can consume live homepage content safely, but the code-managed homepage remains the deterministic source of truth when the database contract is empty or broken.</done>
</task>

<task type="auto">
  <name>Task 3: Add a minimal admin visibility surface for operator audit events</name>
  <read_first>
    - apps/admin/lib/admin-data.ts
    - apps/admin/app/(dashboard)/settings/page.tsx
    - apps/admin/lib/audit-log.ts
    - .planning/workstreams/web-platform/STATE.md
  </read_first>
  <files>apps/admin/lib/admin-data.ts, apps/admin/app/(dashboard)/settings/page.tsx</files>
  <action>Add a focused helper that returns recent operator-specific audit rows by filtering `admin_audit_logs` metadata from Task 1, then render a small section in Settings that distinguishes OpenClaw/operator actions from the broader admin stream. Keep this visibility read-only and lightweight: reuse the existing settings page and audit table patterns instead of creating a new operator dashboard or chat UI. If a small schema-free label is enough, prefer that over a more ambitious UI.</action>
  <verify>
    <automated>npm run admin:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Settings shows recent operator actions separately enough for MVP debugging and audit review.
    - No new admin route or chat surface is introduced.
    - Operator audit entries remain visible through the same `admin_audit_logs` source used elsewhere.
  </acceptance_criteria>
  <done>Admin has a minimal but usable visibility seam for OpenClaw-triggered mutations without expanding scope into a second operator UI.</done>
</task>

</tasks>

<verification>
Run the new homepage resolver test suite plus web typechecks:

```bash
node --test --import tsx apps/site/lib/homepage-content.test.ts
npm run site:typecheck
npm run admin:typecheck
```
</verification>

<success_criteria>
Homepage overrides are delivered through Supabase with safe fallback, operator audit metadata is recorded through the existing audit table, and admin can inspect recent operator actions without a separate operator interface.
</success_criteria>

<output>
After completion, create `.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-01-SUMMARY.md`.
</output>
