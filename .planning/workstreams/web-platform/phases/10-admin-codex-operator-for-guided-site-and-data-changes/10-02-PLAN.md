---
phase: 10-admin-codex-operator-for-guided-site-and-data-changes
plan: "02"
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - package.json
  - .env.example
  - packages/openclaw-everybible/package.json
  - packages/openclaw-everybible/tsconfig.json
  - packages/openclaw-everybible/openclaw.plugin.json
  - packages/openclaw-everybible/src/index.ts
  - packages/openclaw-everybible/src/env.ts
  - packages/openclaw-everybible/src/supabase.ts
  - packages/openclaw-everybible/src/tools/homepage.ts
  - packages/openclaw-everybible/src/tools/operational-summary.ts
  - packages/openclaw-everybible/src/tools/code-change-request.ts
  - packages/openclaw-everybible/src/index.test.ts
  - config/openclaw/everybible-gateway.example.json
  - docs/openclaw/everybible-operator-setup.md
  - .planning/workstreams/web-platform/operator-change-requests/.gitkeep
autonomous: true
requirements:
  - COPILOT-01
  - COPILOT-02
  - COPILOT-03
  - COPILOT-05
  - COPILOT-06
user_setup:
  - service: telegram
    why: "OpenClaw DM access for Jeremy"
    env_vars:
      - name: EVERYBIBLE_OPERATOR_TELEGRAM_BOT_TOKEN
        source: "BotFather -> HTTP API token"
      - name: EVERYBIBLE_OPERATOR_TELEGRAM_ALLOW_FROM
        source: "Jeremy's numeric Telegram user ID or approved group/topic IDs"
    dashboard_config:
      - task: "Create the bot, enable DM use, and record the allowlist IDs used in the example gateway config"
        location: "Telegram BotFather / Telegram account settings"
  - service: codex
    why: "ACP-backed code escalation for source-code changes"
    env_vars: []
    dashboard_config:
      - task: "Ensure the host can run an authenticated Codex/ACP harness session before enabling the escalation tool"
        location: "Codex CLI / local ACP host environment"
must_haves:
  truths:
    - "Jeremy can reach the EveryBible operator from Telegram through a documented OpenClaw gateway configuration."
    - "The operator uses explicit EveryBible tools for live mutations and never falls back to raw SQL or direct filesystem editing."
    - "Source-code change requests create a reviewable Codex/Git/GitHub-style artifact instead of silently mutating production code."
    - "Operator write actions produce audit evidence tied to the same admin audit table introduced in earlier phases."
  artifacts:
    - path: "packages/openclaw-everybible/openclaw.plugin.json"
      provides: "Native OpenClaw plugin manifest for EveryBible tools"
    - path: "packages/openclaw-everybible/src/index.ts"
      provides: "Plugin entrypoint and tool registration"
    - path: "packages/openclaw-everybible/src/tools/code-change-request.ts"
      provides: "Reviewable code-change routing rather than hidden mutation"
    - path: "config/openclaw/everybible-gateway.example.json"
      provides: "Pinned Telegram, memory, plugin, and ACP gateway bootstrap config"
    - path: "docs/openclaw/everybible-operator-setup.md"
      provides: "Human setup/runbook for Telegram, memory, ACP Codex, and verification"
  key_links:
    - from: "packages/openclaw-everybible/src/tools/homepage.ts"
      to: "public.get_live_homepage_content / homepage override table"
      via: "service-role helper and typed contract from 10-01"
      pattern: "get_live_homepage_content|site_content"
    - from: "packages/openclaw-everybible/src/tools/homepage.ts"
      to: "public.admin_audit_logs"
      via: "shared operator audit metadata"
      pattern: "admin_audit_logs"
    - from: "packages/openclaw-everybible/src/tools/code-change-request.ts"
      to: ".planning/workstreams/web-platform/operator-change-requests"
      via: "timestamped markdown artifact"
      pattern: "operator-change-requests"
    - from: "config/openclaw/everybible-gateway.example.json"
      to: "packages/openclaw-everybible/openclaw.plugin.json"
      via: "plugin enablement plus Telegram and ACP settings"
      pattern: "telegram|acp|plugin"
---

<objective>
Add the in-repo OpenClaw operator package, narrow EveryBible tools, and the setup assets that let Telegram, memory, and ACP Codex run as a safe operator workflow.

Purpose: Deliver the actual operator runtime shape after the shared homepage/audit contract exists.
Output: A native OpenClaw plugin workspace package, a reviewable code-change routing tool, a gateway config template, and setup docs covering Telegram, memory, and ACP Codex.
</objective>

<execution_context>
@/Users/dev/.codex/get-shit-done/workflows/execute-plan.md
@/Users/dev/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/web-platform/PROJECT.md
@.planning/workstreams/web-platform/ROADMAP.md
@.planning/workstreams/web-platform/STATE.md
@.planning/workstreams/web-platform/REQUIREMENTS.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-CONTEXT.md
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-RESEARCH.md
@docs/plans/2026-04-02-everybible-codex-operator-design.md
@package.json
@.env.example
@apps/admin/lib/audit-log.ts
@apps/admin/lib/admin-data.ts
@packages/env/src/index.ts
@packages/types/src/index.ts
@.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-01-PLAN.md

<interfaces>
From apps/admin/lib/admin-data.ts:
```ts
export async function getDashboardSummary(): Promise<DashboardSummary>;
export async function getRecentAuditLogs(limit = 12): Promise<AuditLogRow[]>;
export async function listTranslations(searchTerm?: string): Promise<TranslationListItem[]>;
export async function getHealthIssues(): Promise<HealthIssue[]>;
```

From package.json:
```json
{
  "workspaces": ["apps/*", "packages/*"]
}
```

From 10-01 outputs expected by this plan:
```ts
export interface HomepageContentOverridePayload { ... }
export interface OperatorAuditMetadata { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the native EveryBible OpenClaw plugin package and explicit tool surface</name>
  <read_first>
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-RESEARCH.md
    - docs/plans/2026-04-02-everybible-codex-operator-design.md
    - package.json
    - packages/types/src/index.ts
    - apps/admin/lib/admin-data.ts
    - apps/admin/lib/audit-log.ts
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-01-PLAN.md
  </read_first>
  <files>package.json, packages/openclaw-everybible/package.json, packages/openclaw-everybible/tsconfig.json, packages/openclaw-everybible/openclaw.plugin.json, packages/openclaw-everybible/src/index.ts, packages/openclaw-everybible/src/env.ts, packages/openclaw-everybible/src/supabase.ts, packages/openclaw-everybible/src/tools/homepage.ts, packages/openclaw-everybible/src/tools/operational-summary.ts, packages/openclaw-everybible/src/tools/code-change-request.ts, packages/openclaw-everybible/src/index.test.ts</files>
  <behavior>
    - Test 1: homepage read/update tools only accept the approved homepage payload contract and write operator audit metadata on mutation.
    - Test 2: operational summary tools are read-only wrappers around existing EveryBible data helpers and never perform writes.
    - Test 3: code-change escalation creates a reviewable artifact and returns next-step guidance instead of mutating source code or production data.
  </behavior>
  <action>Create a new workspace package named `@everybible/openclaw-everybible` that contains the native OpenClaw plugin manifest, a typed plugin entrypoint, and explicit EveryBible tools only. Implement the MVP tool surface from the phase context: `get_homepage_content`, `update_homepage_content`, `get_content_health_summary`, `get_translation_summary`, `list_recent_admin_actions`, and `request_code_change`. Use service-role helpers inside the package instead of importing Next app aliases. Wire mutation tools to the homepage contract from 10-01 and to `admin_audit_logs` with `actorSource='openclaw'`/Telegram metadata. Make `request_code_change` write a timestamped Markdown artifact into `.planning/workstreams/web-platform/operator-change-requests/` with the requested behavior, likely files, and verification expectations; do not let this tool edit repository files or call GitHub automatically in MVP. Add root scripts for operator typecheck/test so verification stays explicit.</action>
  <verify>
    <automated>npm run operator:typecheck && npm run operator:test</automated>
  </verify>
  <acceptance_criteria>
    - The repo contains a native OpenClaw plugin package that can be installed/enabled without reaching into app-local aliases.
    - Live mutation tools are limited to approved EveryBible surfaces and produce audit rows.
    - Code-change requests produce reviewable artifacts instead of hidden source-code changes.
  </acceptance_criteria>
  <done>An installable in-repo OpenClaw plugin exists with a narrow audited tool surface and a safe code-escalation path.</done>
</task>

<task type="auto">
  <name>Task 2: Add operator bootstrap assets and the setup/runbook for Telegram, memory, and ACP Codex</name>
  <read_first>
    - .env.example
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-CONTEXT.md
    - .planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-RESEARCH.md
    - docs/plans/2026-04-02-everybible-codex-operator-design.md
  </read_first>
  <files>.env.example, config/openclaw/everybible-gateway.example.json, docs/openclaw/everybible-operator-setup.md, .planning/workstreams/web-platform/operator-change-requests/.gitkeep</files>
  <action>Document and scaffold the operator bootstrap path in-repo: extend `.env.example` with the OpenClaw/Telegram/operator env keys the new package needs; add a pinned example gateway config showing Telegram DM pairing/allowlist, plugin enablement, memory configuration, and ACP Codex defaults from the research; and write a concise setup/runbook under `docs/openclaw/` that covers install, pairing approval, Telegram verification, optional Honcho memory enablement, ACP/Codex verification, and safe restart/health-check commands. Keep the docs honest about what remains manual. Do not add a production deployment system or a public-facing admin chat surface in this phase.</action>
  <verify>
    <automated>npm run operator:typecheck && npm run operator:test && npm run admin:typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The repo contains a concrete setup path for Telegram, memory, and ACP Codex that matches the current OpenClaw research.
    - Required operator env vars are discoverable in `.env.example`.
    - The code-change request directory exists so the operator can route code work into a reviewable workflow immediately.
  </acceptance_criteria>
  <done>Jeremy has an in-repo bootstrap/runbook for bringing up the EveryBible OpenClaw operator with Telegram access, durable memory, and Codex-backed escalation.</done>
</task>

</tasks>

<verification>
Run the operator package checks after the new workspace package is added:

```bash
npm run operator:typecheck
npm run operator:test
npm run admin:typecheck
```
</verification>

<success_criteria>
The repo contains an installable OpenClaw plugin package, a safe audited tool surface, a reviewable code-change routing path, and a documented gateway setup for Telegram, memory, and ACP Codex.
</success_criteria>

<output>
After completion, create `.planning/workstreams/web-platform/phases/10-admin-codex-operator-for-guided-site-and-data-changes/10-02-SUMMARY.md`.
</output>
