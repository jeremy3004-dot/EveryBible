# Learn / Gather tab health check (2026-09-24)

Scope: GatherScreen, FoundationDetailScreen, LessonDetailScreen, LessonBottomSheet,
`gatherFoundations.ts`, `gatherWisdom.ts`, `fourFieldsCourses.ts`, `gatherStore`,
`fourFieldsStore`, `gatherBibleService`, the Learn stack and its entry points.
Branch base: origin/main 195717e1. Nothing here was checked on a device or simulator.

## Fixed on this branch (test-first)

| Sev  | Finding                                                                                                                                                                                                                                                                                                                                                                                 | Fix                                                                                                                                                                                                                                 | Test                                                       |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| High | Home's Gather card opened `Learn > FoundationDetail` without `initial: false`. If the Learn tab had not been opened yet, its stack held FoundationDetail alone, so Back left the tab. After that, every tap on Learn reopened that foundation, and the Gather home could not be reached until the app restarted.                                                                        | `navigation/learnRoutes.ts` `gatherFoundationRoute()`                                                                                                                                                                               | `learnRoutes.test.ts`                                      |
| High | Blank Story section: when the reading translation had no text for the passage (for example a New Testament-only translation on a Genesis lesson), `getPassageText` returned a block with no verses. That got past the empty-state check (`blocks.length === 0`) and the screen showed an empty paragraph. If the translation's text pack could not be opened, the whole passage failed. | `getPassageText(..., { fallbackTranslationId })`; the lesson passes the bundled BSB. `lessonPassageModel` puts the source translation's name above each borrowed block, and the hero eyebrow names only the translations on screen. | `gatherBibleService.test.ts`, `lessonPassageModel.test.ts` |
| Med  | After the story audio finished, Play and "Listen to the story again" did nothing. expo-av leaves a finished sound at its end position on iOS (`EXAVPlayerData` pauses there) and on Android (ExoPlayer `STATE_ENDED`).                                                                                                                                                                  | `lessonAudioModel.readLessonPlaybackStatus` rewinds when playback finishes                                                                                                                                                          | `lessonAudioModel.test.ts`                                 |
| Med  | A corrupted `gather-storage` slot was loaded without sanitising, and the first `isLessonComplete` threw a `TypeError`, which crashed the Gather tab and the Home Gather card. The earlier test recorded this as a QUESTION.                                                                                                                                                             | `sanitizePersistedGatherState` + persist `merge`                                                                                                                                                                                    | `gatherStore.test.ts`                                      |
| Med  | The up-next card on Gather home used the data file's English `referenceLabel` ("Genesis 1"), while every other Gather surface translates the book name.                                                                                                                                                                                                                                 | `gatherPathModel.resolveGatherUpNext`                                                                                                                                                                                               | `gatherPathModel.test.ts`                                  |
| Low  | Fixed sizes that clip at large text sizes: the path-row count was `width: 30` with one line, so "10/10" was cut to "1…". The FoundationDetail and LessonDetail headers had fixed `height`.                                                                                                                                                                                              | Changed to `minWidth` / `minHeight`                                                                                                                                                                                                 | UI only; no renderer is available                          |
| Low  | Accessibility: the Gather title and the Lesson Story/Application headings had no header role. The lesson progress bar was marked `adjustable` but had no increment/decrement actions.                                                                                                                                                                                                   | Added the roles, 10 s seek actions, and a time value                                                                                                                                                                                | UI only                                                    |

New data-integrity test `src/data/learnScripture.integrity.test.ts` checks every Foundations, Wisdom and Four Fields reference (every chapter:verse endpoint) against `assets/databases/bible-bsb-v2.db`, and checks each Wisdom `lessonCount` against the lessons it actually contains. **All current data passes.** Every reference resolves in the shipped BSB.

## Not fixed (needs a decision or out of scope)

- **High (licensing, latent):** 19 of the 29 Four Fields key verses in `fourFieldsCourses.ts` are NIV wording, not BSB (for example Col 3:16 "Let the message of Christ dwell among you richly", 1 Cor 14:26 "brothers and sisters"). Quoting NIV requires its copyright notice. Before anything renders this content, replace the text with BSB or read it from the bundled DB.
- **Med (latent):** The Four Fields course content (section text, takeaways, discussion questions, practice activities, key-verse references) is English only and has no translation keys. It is rendered only by GroupDetail and GroupSession, and no screen navigates to `GroupList`, so users cannot reach it today. GroupSession also builds colours by concatenating hex strings (`colors.warning + '15'`).
- **Med:** LessonBottomSheet's "Share audio", "Share text" and "Share link" all share the same `title - reference` string. None of them shares audio, the passage text or a link.
- **Low:** A passage that fails to load shows the same "No passage text available" message as a passage that is genuinely empty, with no retry. The BSB fallback makes this rare.
- **Low:** When the story falls back to BSB, audio still resolves for the reading translation, so Play stays disabled for a New Testament-only translation on an Old Testament lesson.
- **Low:** FoundationDetail's `completed/total` counts every stored id, including ids no longer in the catalog, so it can show 11/10 (GatherScreen caps its count). Count only the lessons in the catalog.
- **Info:** `FELLOWSHIP_QUESTIONS` and `APPLICATION_QUESTIONS` in `gatherFoundations.ts` are English copies used only by tests. The screen uses the `gather.*Q*` keys.
- **Info:** No deep links go into lessons. `linkingConfig` covers only `bible/…` and `reset-password`. An unknown `lessonId`/`foundationId` shows a not-found state with a working Back button.

## Checked and fine

- **Progress scoping:** Both `gather-storage` (lesson marks) and `four-fields-storage` are in `PRIVATE_DATA_STORE_NAMES` and are registered with `registerPrivateDataStore`. Marks are stored per account, merged from guest on first sign-in, and survive restarts through MMKV. `privateDataAccounts.test.ts` covers switching accounts.
- **Sync:** Gather marks and Four Fields progress are local only and never synced. That matches the privateDataScope decision, but the CLAUDE.md line "Progress Tracking … syncs to Supabase" is out of date for course progress.
- **Mark/unmark:** Toggling works from both the lesson capsule and the bottom sheet, and GatherScreen subscribes to `completedLessons`, so counts update everywhere.
- **Non-English users:** Every foundation, wisdom and lesson title and every question comes from `gather.*` keys present in all 21 locales, which the coverage tests enforce. Book names are translated. The Bible text follows the reading translation, with the labelled BSB fallback above.
- **Colours:** No hard-coded colours in the Gather screens or components.
