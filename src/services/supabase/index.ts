export { supabase, isSupabaseConfigured, getCurrentUserId } from './client';
export type {
  Database,
  GroupMemberRecord,
  GroupRecord,
  GroupSessionRecord,
  Profile,
  UserProgress,
  UserPreferences,
  Tables,
  InsertTables,
  UpdateTables,
} from './types';
