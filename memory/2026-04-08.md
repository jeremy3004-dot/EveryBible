## 2026-04-08

- User wants token-saving behavior to be the default in every session and chat unless they explicitly ask otherwise.
- Bias toward smaller/scoped model usage, concise replies, and minimal tool/output overhead by default.
