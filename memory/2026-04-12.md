# 2026-04-12

- Generated the full BSB verse timestamp corpus and regenerated the bundled timestamp map.
- Confirmed the remote publish target is Cloudflare R2 bucket `everybibleapp` at `https://9ebfac5a12f408afc1d80eaa2138ffd3.r2.cloudflarestorage.com`.
- Learned that R2 syncs need explicit AWS credential injection in the command environment; ambient shell credentials can be stale or incorrect.
- Do not save raw access keys or secrets in memory files.
- Locked in an EveryBible reader invariant: for plan/day sessions in read mode, use the exact same floating playback dock as the regular reader above the red banner, and do not move the play button into the banner unless explicitly requested.
- Added a Phase 15 research addendum on scripture readability mechanics in `.planning/phases/15-reverential-theme-typography/15-RESEARCH.md`, covering prose measure, leading, poetry indentation, verse-number restraint, and why ESV-style layout feels easier to read.
- Added a focused reader layout spec (`docs/plans/2026-04-12-reader-text-layout-spec.md`) and Phase 15 execute plan (`15-04-PLAN.md`), then implemented premium reader inline paragraph flow plus tighter scripture typography defaults. Verification passed for targeted reader tests and lint on touched files; full `npm run typecheck` is currently blocked by an unrelated `TabNavigator.tsx` / `BibleState` mismatch around `isPlanSessionReaderActive`.
- Restored `isPlanSessionReaderActive` to the Bible store, unblocked typecheck, and carried real BSB line/poetry metadata end-to-end through `bsb_processed.json`, the bundled SQLite asset, database readers/writers, and the reader renderer.
- Rebuilt `assets/databases/bible-bsb-v2.db` on schema version 5 and confirmed 9,664 verses now carry non-null formatting payloads for preserved poetry/line-break layout.
